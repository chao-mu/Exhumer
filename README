Exhumer is a framework for finding and extracting data from remote sources, someday supporting everything from SSH servers to Search Engines

PRE ALPHA EXPERIMENTAL VERSION! Don't expect anything at all to work.

Example commands:
bin/hunt.rb presets/crypt --config foo.baz=bar,foo.qux=quux,refresh=true
